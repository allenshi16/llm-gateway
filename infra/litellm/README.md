# LiteLLM data plane

This directory contains generated configuration only. Do not copy LiteLLM source here or expose the proxy publicly. Add approved model routes and the separately packaged auth/callback adapter through deployment configuration. `LITELLM_IMAGE` must be a verified immutable digest before any production deployment; the local example is intentionally a placeholder and must fail the deployment gate until replaced.
