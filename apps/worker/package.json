{
  "name": "@gateway/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {"build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "dev": "tsx watch src/index.ts"},
  "dependencies": {"@gateway/core": "workspace:*", "@gateway/database": "workspace:*", "@gateway/contracts": "workspace:*"}
}
