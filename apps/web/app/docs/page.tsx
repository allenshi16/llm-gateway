import { ContentCard, MarketingPage } from "../marketing-page";
export const metadata = { title: "LLM API documentation", description: "Documentation for the Maridian Gateway OpenAI-compatible API." };
export default function DocsPage() { return <MarketingPage eyebrow="DOCS / OPENAI-COMPATIBLE" title="Connect once. Route with intent." intro="Use familiar chat completion semantics while the gateway handles approved model routes and commercial policy."><ContentCard title="Base URL">Your application calls the gateway endpoint with a workspace API key and a model name enabled for that workspace.</ContentCard><ContentCard title="Authentication">Create a live key in Console, store it server-side, and send it as a bearer token. Never expose it in browser code.</ContentCard><ContentCard title="Next step">The public API reference is being expanded alongside the production Edge contract.</ContentCard></MarketingPage>; }
